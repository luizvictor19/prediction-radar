// Stub — implemented in Week 5-6 per roadmap
import type { DetectedSignal } from '../types/index.js';

export async function sendSignalAlert(_signal: DetectedSignal): Promise<void> {
  console.log('[telegram] Alert not yet configured.');
}
